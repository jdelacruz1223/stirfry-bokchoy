using UnityEngine;

public class Credits : MonoBehaviour
{
    [SerializeField] GameObject backToMenuCanvas;

    void Start()
    {
        // get all the children
        // move them apart vertically by a variable amount
        // move them all down so that the first one starts invisible
        // start scrolling down by a variable speed
        // detect when the last bubble goes off screen
        // make backToMenuCanvas appear
    }

    void Update()
    {
        
    }
}
